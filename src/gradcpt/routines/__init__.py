"""PsychoPy-dependent routines: trial loop, probes, instruction screens.

These modules import :mod:`psychopy` lazily inside their functions so the
top-level package remains importable without it.
"""
